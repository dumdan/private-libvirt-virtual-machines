---
- name : Play 01 - setup
  hosts: gcpsdk
  become: True
  gather_facts: True

  tasks:
    - name: execute "roles" playbooks
      include_role:
        name: "{{ my_role }}"
      loop:
          - roles/docker
      loop_control:
        loop_var: my_role


